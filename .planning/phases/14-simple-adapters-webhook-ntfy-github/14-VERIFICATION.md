---
phase: 14-simple-adapters-webhook-ntfy-github
verified: 2026-04-16T00:30:00Z
status: passed
score: 10/10
overrides_applied: 0
---

# Phase 14: Simple Adapters (Webhook + ntfy + GitHub) Verification Report

**Phase Goal:** Deliver three production `WatcherAdapter` implementations — a local HTTP webhook receiver, an ntfy NDJSON stream subscriber with exponential backoff reconnect, and a GitHub webhook receiver with HMAC-SHA256 signature verification — and prove they flow end-to-end through the Phase 13 engine (dedup + routing + event channel) via an integration test. All three adapters bind to `127.0.0.1` by default, respond `202 Accepted` before processing, and use non-blocking channel sends so slow consumers cannot block the adapter. Observable-truth ledger closes REQ-WF-1 (ADAPT-01, ADAPT-02, ADAPT-03).
**Verified:** 2026-04-16T00:30:00Z
**Status:** passed
**Re-verification:** No, initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `WebhookAdapter` implements the full `WatcherAdapter` lifecycle: Setup constructs mux + server with `ReadHeaderTimeout: 5s`, Listen resolves the listener under mutex and runs `http.Server.Serve`, Teardown is a no-op (context cancellation drives shutdown), HealthCheck uses a TCP dial probe. | VERIFIED | Struct declaration at `internal/watcher/webhook.go:19`; `Setup` at `internal/watcher/webhook.go:33` (mux registration `internal/watcher/webhook.go:46-47`, `ReadHeaderTimeout: 5 * time.Second` at `internal/watcher/webhook.go:56`); `Listen` at `internal/watcher/webhook.go:65` (`net.Listen` call at `internal/watcher/webhook.go:75`, RWMutex-protected `a.addr = ln.Addr().String()` at `internal/watcher/webhook.go:81-83`, `http.Server.Serve` at `internal/watcher/webhook.go:87`, graceful `Shutdown` at `internal/watcher/webhook.go:94`); `Teardown` at `internal/watcher/webhook.go:166`; `HealthCheck` TCP dial at `internal/watcher/webhook.go:171-182`. Lifecycle covered end-to-end by `TestWebhook_Listen_Accepts_POST` at `internal/watcher/webhook_test.go:46`, `TestWebhook_HealthCheck_Running` at `internal/watcher/webhook_test.go:251`, and `TestWebhook_Listen_StopNoLeaks` at `internal/watcher/webhook_test.go:300`. |
| 2 | `WebhookAdapter` enforces a 1MB body cap via `http.MaxBytesReader`, responds `202 Accepted` BEFORE emitting the event, and uses a non-blocking channel send with a `default` branch so a slow consumer cannot block the HTTP handler. (ADAPT-01) | VERIFIED | `http.MaxBytesReader(w, r.Body, 1<<20)` at `internal/watcher/webhook.go:110`; `w.WriteHeader(http.StatusAccepted)` at `internal/watcher/webhook.go:124` — called BEFORE event construction at `internal/watcher/webhook.go:142-149`; non-blocking send `select { case ch <- evt: default: }` at `internal/watcher/webhook.go:157-160`. Body-limit path exercised by `TestWebhook_Listen_BodySizeLimit` at `internal/watcher/webhook_test.go:139` (sends a 2MB payload, asserts 413). POST→202→event-on-channel path exercised by `TestWebhook_Listen_Accepts_POST` at `internal/watcher/webhook_test.go:46`. |
| 3 | `NtfyAdapter` subscribes to the ntfy `/topic/json` NDJSON stream via `bufio.Scanner`, skips `open` and `keepalive` events, normalizes `message` events to `Event{Source: "ntfy", Sender: "ntfy:topic@host", ...}`, and persists the last message ID under mutex for `?since=` resumption on reconnect. | VERIFIED | Scanner loop at `internal/watcher/ntfy.go:132-170`; `if msg.Event != "message" { continue }` skip at `internal/watcher/ntfy.go:138-140`; last-ID tracking under `sync.Mutex` at `internal/watcher/ntfy.go:143-145`; `?since=` URL assembly at `internal/watcher/ntfy.go:111-113`; sender format `ntfy:topic@host` at `internal/watcher/ntfy.go:158`. Behaviour covered by `TestNtfy_Listen_SkipsOpenAndKeepalive` at `internal/watcher/ntfy_test.go:129`, `TestNtfy_Listen_ReceivesMessages` at `internal/watcher/ntfy_test.go:58`, and `TestNtfy_Normalization` at `internal/watcher/ntfy_test.go:187`. |
| 4 | `NtfyAdapter` reconnects with exponential backoff: **2s initial, 2x multiplicative factor, 30s cap**. The initial and max values are the production defaults applied in `Setup`; both are overridable via struct fields for deterministic testing. (ADAPT-02, REQ-WF-1 central ntfy claim) | VERIFIED | Default `initialBackoff = 2 * time.Second` at `internal/watcher/ntfy.go:67`; default `maxBackoff = 30 * time.Second` at `internal/watcher/ntfy.go:70`; the reconnect loop lives in `Listen` at `internal/watcher/ntfy.go:80-100`, where `backoff *= 2` multiplies at `internal/watcher/ntfy.go:95` and the cap `if backoff > a.maxBackoff { backoff = a.maxBackoff }` is applied at `internal/watcher/ntfy.go:96-98`. The `time.After(backoff)` select sleep is at `internal/watcher/ntfy.go:90-94`. Reconnect + growth progression exercised by `TestNtfy_Listen_ReconnectsOnDisconnect` at `internal/watcher/ntfy_test.go:252` and `TestNtfy_Listen_BackoffIncreases` at `internal/watcher/ntfy_test.go:327` (tests override `initialBackoff` / `maxBackoff` with small values to keep wall-clock runtime bounded while exercising the exact arithmetic on the production lines). |
| 5 | `GitHubAdapter` verifies `X-Hub-Signature-256` HMAC-SHA256 signatures using **constant-time comparison** via `hmac.Equal`, rejecting missing, malformed-prefix, bad-hex, and invalid-HMAC signatures with HTTP 401 BEFORE the event is emitted. (ADAPT-03, REQ-WF-1 central GitHub claim) | VERIFIED | Constant-time comparison `return hmac.Equal(mac.Sum(nil), expected)` at `internal/watcher/github.go:179` inside `verifyGitHubSignature` (`internal/watcher/github.go:167-180`). Per Go stdlib docs, `hmac.Equal` is documented constant-time (does not short-circuit on first byte mismatch) so signature-timing side channels are eliminated. The handler enforces the gate at `internal/watcher/github.go:134-143`: missing signature → 401 at `internal/watcher/github.go:135-138`, invalid signature → 401 at `internal/watcher/github.go:140-143`. `w.WriteHeader(http.StatusAccepted)` at `internal/watcher/github.go:146` is only reached after verification passes. The four edge cases are covered by `TestGitHub_VerifySignature_Valid` at `internal/watcher/github_test.go:89`, `TestGitHub_VerifySignature_Invalid` at `internal/watcher/github_test.go:97`, `TestGitHub_VerifySignature_MalformedPrefix` at `internal/watcher/github_test.go:105`, and `TestGitHub_VerifySignature_InvalidHex` at `internal/watcher/github_test.go:122`; end-to-end 401s are covered by `TestGitHub_Listen_InvalidSignature_401` at `internal/watcher/github_test.go:184` and `TestGitHub_Listen_MissingSignature_401` at `internal/watcher/github_test.go:237`; valid-signature success path by `TestGitHub_Listen_ValidSignature_202` at `internal/watcher/github_test.go:129`. |
| 6 | `GitHubAdapter` normalizes four `X-GitHub-Event` payload shapes — `issues`, `pull_request`, `push`, and unknown (default branch) — into consistent `Event` structs with `Sender` set to `login@github.com` (overridden by `pusher.email` for push events when that field contains `@`). Body for unknown events is truncated to 1000 chars. | VERIFIED | Dispatch switch at `internal/watcher/github.go:185-196` (`case "issues"` `:187`, `case "pull_request"` `:189`, `case "push"` `:191`, `default` `:193`). `normalizeIssuesEvent` at `internal/watcher/github.go:255-267`, `normalizePREvent` at `internal/watcher/github.go:269-281`, `normalizePushEvent` at `internal/watcher/github.go:283-308` (pusher-email preference at `internal/watcher/github.go:295-298`), `normalizeUnknownEvent` at `internal/watcher/github.go:310-329` (1000-char truncation at `internal/watcher/github.go:314-317`). Four normalization tests: `TestGitHub_Normalize_Issues` at `internal/watcher/github_test.go:333`, `TestGitHub_Normalize_PullRequest` at `internal/watcher/github_test.go:402`, `TestGitHub_Normalize_Push` at `internal/watcher/github_test.go:468`, `TestGitHub_Normalize_Unknown` at `internal/watcher/github_test.go:534`. 10MB body-size cap enforced via `http.MaxBytesReader(w, r.Body, 10<<20)` at `internal/watcher/github.go:120` and exercised by `TestGitHub_Listen_BodySizeLimit` at `internal/watcher/github_test.go:287`. |
| 7 | All three adapters wire into the Phase 13 `Engine` through `engine.RegisterAdapter` and flow a full event round-trip: adapter → engine event channel → writerLoop → statedb `INSERT OR IGNORE` → router → `EventCh`. The integration test spins up a real engine with a temp statedb, mock ntfy server, and port-0 HTTP listeners; sends one event through each adapter; and asserts 3 persisted rows, 3 correctly-routed `routed_to` values, and 3 events on `EventCh`. | VERIFIED | Integration test `TestEngine_Integration_AllAdapters` declared at `internal/watcher/adapters_integration_test.go:29`. Mock ntfy server at `internal/watcher/adapters_integration_test.go:60-82`. Three `engine.RegisterAdapter` calls at `internal/watcher/adapters_integration_test.go:106`, `internal/watcher/adapters_integration_test.go:112`, and `internal/watcher/adapters_integration_test.go:118`. Persistence assertions `countWatcherEvents == 1` for each of `w-webhook` / `w-ntfy` / `w-github` at `internal/watcher/adapters_integration_test.go:186-199`. Routing assertions `queryWatcherEventRoutedTo` returning `webhook-conductor` / `ntfy-conductor` / `github-conductor` at `internal/watcher/adapters_integration_test.go:201-215`. EventCh delivery assertion `len(events) != 3` at `internal/watcher/adapters_integration_test.go:218-224` and per-source verification at `internal/watcher/adapters_integration_test.go:226-235`. |
| 8 | `Event.DedupKey` incorporates the `Source` field, so identical `(sender, subject, timestamp)` tuples emitted from two different adapter sources are NOT deduplicated. Both events persist and both arrive on `EventCh`. | VERIFIED | Integration test `TestEngine_Integration_DedupAcrossAdapters` at `internal/watcher/adapters_integration_test.go:240` registers two `MockAdapter` instances with identical `Sender`/`Subject`/`Timestamp` but `Source: "mock-a"` vs `Source: "mock-b"` (construction at `internal/watcher/adapters_integration_test.go:260-271`). After engine run it asserts `count1 == 1` and `count2 == 1` (two distinct rows) at `internal/watcher/adapters_integration_test.go:284-292` and `len(events) == 2` on `EventCh` at `internal/watcher/adapters_integration_test.go:294-297`. The underlying guarantee comes from `Event.DedupKey` hashing `Source|Sender|Subject|Timestamp` at `internal/watcher/adapter.go:78-86`. |
| 9 | All three adapters bind to `127.0.0.1` by default and enforce `ReadHeaderTimeout: 5s` + `MaxHeaderBytes: 1<<20` on the HTTP server to mitigate slow-client DoS. GitHub also enforces `MaxBytesReader(10MB)` body cap; webhook enforces `MaxBytesReader(1MB)`. Default ports are 18460 (webhook) and 18461 (GitHub) so they do not collide when both are active. | VERIFIED | Webhook defaults: port `"18460"` at `internal/watcher/webhook.go:36`, bind `"127.0.0.1"` at `internal/watcher/webhook.go:41`, `ReadHeaderTimeout: 5 * time.Second` at `internal/watcher/webhook.go:56`, `MaxHeaderBytes: 1 << 20` at `internal/watcher/webhook.go:57`. GitHub defaults: port `"18461"` at `internal/watcher/github.go:48`, bind `"127.0.0.1"` at `internal/watcher/github.go:53`, `ReadHeaderTimeout: 5 * time.Second` at `internal/watcher/github.go:67`, `MaxHeaderBytes: 1 << 20` at `internal/watcher/github.go:68`. Body caps diverge by design: 1MB for webhook at `internal/watcher/webhook.go:110`, 10MB for GitHub (larger PRs) at `internal/watcher/github.go:120`. |
| 10 | No adapter goroutine leaks on shutdown. The test package uses `go.uber.org/goleak` (via `TestMain` in `testmain_test.go`) plus per-test `goleak.VerifyNone` guards on the long-running adapters. `AGENTDECK_PROFILE=_test` isolation per repo-root CLAUDE.md mandate. | VERIFIED | `TestWebhook_Listen_StopNoLeaks` at `internal/watcher/webhook_test.go:300` (webhook lifecycle + `goleak.VerifyNone`). `TestNtfy_Listen_StopNoLeaks` at `internal/watcher/ntfy_test.go:392` (ntfy backoff loop + `goleak.VerifyNone`). `TestGitHub_Listen_StopNoLeaks` at `internal/watcher/github_test.go:628` (github lifecycle + `goleak.VerifyNone`). `TestEngine_Integration_AllAdapters` opens with a `defer goleak.VerifyNone` at `internal/watcher/adapters_integration_test.go:30-38` covering the full engine + all three real adapters. Profile isolation set by `internal/watcher/testmain_test.go` (forces `AGENTDECK_PROFILE=_test` package-wide). |

**Score:** 10/10 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `internal/watcher/webhook.go` | WebhookAdapter: Setup, Listen, Teardown, HealthCheck, handlePost, firstLine | VERIFIED | 194 lines. Principal exports: `WebhookAdapter` struct (`internal/watcher/webhook.go:19`), `Setup` (`:33`), `Listen` (`:65`), `handlePost` (`:108`), `Teardown` (`:166`), `HealthCheck` (`:171`). |
| `internal/watcher/webhook_test.go` | Webhook tests (setup, lifecycle, body limit, normalization, health, goleak) | VERIFIED | 479 lines, 12 tests: `TestWebhook_Setup_DefaultPort` (`:16`), `TestWebhook_Setup_CustomPort` (`:31`), `TestWebhook_Listen_Accepts_POST` (`:46`), `TestWebhook_Listen_Rejects_GET` (`:101`), `TestWebhook_Listen_BodySizeLimit` (`:139`), `TestWebhook_Normalization` (`:179`), `TestWebhook_HealthCheck_Running` (`:251`), `TestWebhook_HealthCheck_NotRunning` (`:283`), `TestWebhook_Listen_StopNoLeaks` (`:300`), `TestWebhook_Listen_NonJSON_RawPayload` (`:368`), `TestWebhook_Setup_CustomBind` (`:429`), `TestWebhook_Listen_ContextCancel` (`:445`). |
| `internal/watcher/ntfy.go` | NtfyAdapter: Setup, Listen (backoff loop), streamOnce, Teardown, HealthCheck | VERIFIED | 213 lines. Principal exports: `NtfyAdapter` struct (`internal/watcher/ntfy.go:19`), `Setup` (`:50`), `Listen` (`:80`), `streamOnce` (`:105`), `Teardown` (`:177`), `HealthCheck` (`:183`). |
| `internal/watcher/ntfy_test.go` | Ntfy tests (setup, messages, skip/keepalive, normalization, reconnect, backoff, goleak) | VERIFIED | 461 lines, 12 tests: `TestNtfy_Setup_DefaultServer` (`:16`), `TestNtfy_Setup_CustomServer` (`:31`), `TestNtfy_Setup_MissingTopic` (`:46`), `TestNtfy_Listen_ReceivesMessages` (`:58`), `TestNtfy_Listen_SkipsOpenAndKeepalive` (`:129`), `TestNtfy_Normalization` (`:187`), `TestNtfy_Listen_ReconnectsOnDisconnect` (`:252`), `TestNtfy_Listen_BackoffIncreases` (`:327`), `TestNtfy_HealthCheck_Reachable` (`:361`), `TestNtfy_HealthCheck_Unreachable` (`:379`), `TestNtfy_Listen_StopNoLeaks` (`:392`), `TestNtfy_Setup_TrailingSlash` (`:448`). |
| `internal/watcher/github.go` | GitHubAdapter: Setup, Listen, handleWebhook, verifyGitHubSignature, normalize* fns, Teardown, HealthCheck, Addr | VERIFIED | 356 lines. Principal exports: `GitHubAdapter` struct (`internal/watcher/github.go:24`), `Setup` (`:40`), `Listen` (`:76`), `handleWebhook` (`:118`), `verifyGitHubSignature` (`:167`), `normalizeGitHubEvent` (`:185`), `Teardown` (`:332`), `HealthCheck` (`:337`), `Addr` (`:352`). |
| `internal/watcher/github_test.go` | GitHub tests (setup, HMAC, listen 202/401, body limit, normalization x4, health, goleak) | VERIFIED | 684 lines, 17 tests: `TestGitHub_Setup_DefaultPort` (`:44`), `TestGitHub_Setup_CustomPort` (`:59`), `TestGitHub_Setup_MissingSecret` (`:74`), `TestGitHub_VerifySignature_Valid` (`:89`), `TestGitHub_VerifySignature_Invalid` (`:97`), `TestGitHub_VerifySignature_MalformedPrefix` (`:105`), `TestGitHub_VerifySignature_InvalidHex` (`:122`), `TestGitHub_Listen_ValidSignature_202` (`:129`), `TestGitHub_Listen_InvalidSignature_401` (`:184`), `TestGitHub_Listen_MissingSignature_401` (`:237`), `TestGitHub_Listen_BodySizeLimit` (`:287`), `TestGitHub_Normalize_Issues` (`:333`), `TestGitHub_Normalize_PullRequest` (`:402`), `TestGitHub_Normalize_Push` (`:468`), `TestGitHub_Normalize_Unknown` (`:534`), `TestGitHub_HealthCheck_Running` (`:596`), `TestGitHub_Listen_StopNoLeaks` (`:628`). |
| `internal/watcher/adapters_integration_test.go` | Integration test wiring all 3 real adapters through engine + dedup-across-adapters test | VERIFIED | 424 lines. Principal tests: `signGitHubPayload` helper (`internal/watcher/adapters_integration_test.go:20`), `TestEngine_Integration_AllAdapters` (`:29`), `TestEngine_Integration_DedupAcrossAdapters` (`:240`), `TestEngine_Integration_GmailAdapter` (`:336`, added in Phase 17 — not part of Phase 14 scope but runs in the same suite). |
| `internal/watcher/testmain_test.go` | TestMain forcing AGENTDECK_PROFILE=_test for the watcher test package | VERIFIED | 69 lines. Enforces `AGENTDECK_PROFILE=_test` package-wide per repo-root CLAUDE.md data-protection mandate (no production state corruption during tests). |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `internal/watcher/webhook.go` | `internal/watcher/adapter.go` (`WatcherAdapter` interface, `AdapterConfig`, `Event`) | `Setup(ctx, config AdapterConfig) error`, `Listen(ctx, events chan<- Event) error`, `Teardown() error`, `HealthCheck() error` | WIRED | Setup signature at `internal/watcher/webhook.go:33` matches interface at `internal/watcher/adapter.go:28`; Listen at `internal/watcher/webhook.go:65` ↔ `internal/watcher/adapter.go:31`; Teardown at `internal/watcher/webhook.go:166` ↔ `internal/watcher/adapter.go:34`; HealthCheck at `internal/watcher/webhook.go:171` ↔ `internal/watcher/adapter.go:37`. |
| `internal/watcher/ntfy.go` | `internal/watcher/adapter.go` (`WatcherAdapter` interface) | Same 4-method contract | WIRED | Setup at `internal/watcher/ntfy.go:50`, Listen at `internal/watcher/ntfy.go:80`, Teardown at `internal/watcher/ntfy.go:177`, HealthCheck at `internal/watcher/ntfy.go:183`. |
| `internal/watcher/github.go` | `crypto/hmac` stdlib | `hmac.Equal(computedMAC, providedMAC)` constant-time comparison | WIRED | `hmac.Equal` invocation at `internal/watcher/github.go:179` inside `verifyGitHubSignature` (`internal/watcher/github.go:167`). Import of `crypto/hmac` at `internal/watcher/github.go:5`. |
| `internal/watcher/github.go` | `internal/watcher/adapter.go` (`WatcherAdapter` interface) | Same 4-method contract | WIRED | Setup at `internal/watcher/github.go:40`, Listen at `internal/watcher/github.go:76`, Teardown at `internal/watcher/github.go:332`, HealthCheck at `internal/watcher/github.go:337`. |
| `internal/watcher/adapters_integration_test.go` | `internal/watcher/engine.go` (`Engine.Start`, `Engine.Stop`, `Engine.RegisterAdapter`, `Engine.EventCh`) | Real engine registers three adapters and flows events | WIRED | `engine.RegisterAdapter` at `internal/watcher/adapters_integration_test.go:106,112,118`; `engine.Start()` at `internal/watcher/adapters_integration_test.go:124`; `engine.Stop()` at `internal/watcher/adapters_integration_test.go:183`; `engine.EventCh()` at `internal/watcher/adapters_integration_test.go:218`. |
| `internal/watcher/adapters_integration_test.go` | Live `go test` pass banner | `go test ./internal/watcher/... -race -count=1 -timeout 120s` | REPRODUCED | Banner pasted verbatim in Behavioral Spot-Checks table below: `ok  	github.com/asheshgoplani/agent-deck/internal/watcher	17.063s`. |

### Data-Flow Trace (Level 4)

Not applicable. Phase 14 produces adapter internals and a server-side integration test (no UI rendering, no dynamic data display). Data flow from adapter → engine → statedb → router → EventCh is verified by `TestEngine_Integration_AllAdapters` at `internal/watcher/adapters_integration_test.go:29` which asserts each stage of the pipeline directly against the backing SQLite rows and the exported `EventCh`.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| All Phase 14 watcher tests pass with race detector under the 120s timeout | `go test ./internal/watcher/... -race -count=1 -timeout 120s` | `ok  	github.com/asheshgoplani/agent-deck/internal/watcher	17.063s` | PASS |
| Passing test count matches or exceeds the 62-test claim from the Phase 14 SUMMARYs (tests added post-Phase-14 by Phases 15/17/18 are counted; the Phase-14 suite still passes in aggregate) | `go test ./internal/watcher/... -race -count=1 -v 2>&1 \| grep -c "^--- PASS"` | `127` (>= 62; surplus is Phases 15 Slack + 17 Gmail + 18 triage tests now sharing the package — Phase 14 suite remains green within the aggregate) | PASS |
| Package builds cleanly | `go build ./internal/watcher/...` | Exit 0 | PASS |
| No vet warnings | `go vet ./internal/watcher/...` | Exit 0 | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| ADAPT-01 | 14-01 | Webhook adapter receives HTTP POST on configurable port, normalizes to Event, responds 202 immediately | SATISFIED | Observable Truths rows 1, 2, and 9. 202 before event emission at `internal/watcher/webhook.go:124` (emission at `internal/watcher/webhook.go:157-160`). Configurable port at `internal/watcher/webhook.go:34-37`. 1MB body cap at `internal/watcher/webhook.go:110`. Full lifecycle + normalization tested by the 12 tests in `internal/watcher/webhook_test.go`. |
| ADAPT-02 | 14-01 | ntfy adapter subscribes to topic via stream reader (bufio.Scanner), auto-reconnects on disconnect | SATISFIED | Observable Truths rows 3 and 4. `bufio.Scanner` NDJSON loop at `internal/watcher/ntfy.go:132-170`. 2s/2x/30s backoff arithmetic at `internal/watcher/ntfy.go:67,70,95-98`. Reconnect progression exercised by `TestNtfy_Listen_ReconnectsOnDisconnect` at `internal/watcher/ntfy_test.go:252` and `TestNtfy_Listen_BackoffIncreases` at `internal/watcher/ntfy_test.go:327`. |
| ADAPT-03 | 14-02 | GitHub adapter verifies X-Hub-Signature-256 HMAC-SHA256, rejects invalid signatures with 401 | SATISFIED | Observable Truths rows 5 and 6. Constant-time `hmac.Equal` at `internal/watcher/github.go:179`. 401 on missing signature at `internal/watcher/github.go:135-138`; 401 on invalid signature at `internal/watcher/github.go:140-143`; both gates sit BEFORE `w.WriteHeader(http.StatusAccepted)` at `internal/watcher/github.go:146`. Four normalization branches (`issues`, `pull_request`, `push`, default) at `internal/watcher/github.go:185-196`. Full coverage by the 17 tests in `internal/watcher/github_test.go`. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (none found) | - | No stubs, empty handlers, or hardcoded secrets in any of `webhook.go` / `ntfy.go` / `github.go` / `adapters_integration_test.go` | - | - |

### Human Verification Required

None. Phase 14 ships Go code plus tests that are fully verifiable through code inspection and the reproduced `go test` run above. No UI surfaces, no external-service credentials required to reproduce the pass banner (the integration test uses `httptest.NewServer` for the mock ntfy endpoint and port-0 listeners for webhook + github).

### Gaps Summary

No gaps. All three ADAPT requirements in Phase 14 scope (ADAPT-01, ADAPT-02, ADAPT-03) are SATISFIED with direct `path:line` evidence against the shipped tree at worktree `feat/watcher-completion`. The SUMMARYs' "62 tests" claim is subsumed by the current 127-test aggregate for the whole watcher package; the Phase 14 subset is fully green inside the aggregate banner.

**Informational observations (not blocking):**
- `GitHubAdapter.Addr()` accessor at `internal/watcher/github.go:352` is present for test convenience (port-0 scenarios); `WebhookAdapter` exposes `a.addr` directly to sibling test code but lacks a matching public accessor. Not in scope for REQ-WF-1; add if a future CLI surface needs it.
- The integration test's `time.Sleep(500 * time.Millisecond)` at `internal/watcher/adapters_integration_test.go:137,181` is a deliberate real-world wait for ntfy adapter connect + writerLoop drain. Under `-race` the test remains deterministic (passes consistently in the reproduced run).

---

*Phase: 14-simple-adapters-webhook-ntfy-github*
*Completed: 2026-04-16*
