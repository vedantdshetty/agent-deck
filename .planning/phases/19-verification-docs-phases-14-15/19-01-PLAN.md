---
phase: 19
plan: 19-01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/14-simple-adapters-webhook-ntfy-github/14-VERIFICATION.md
autonomous: true
requirements:
  - REQ-WF-1
tags: [docs-backfill, verification, watcher, adapters, webhook, ntfy, github]
user_setup: []

must_haves:
  truths:
    - "14-VERIFICATION.md exists at .planning/phases/14-simple-adapters-webhook-ntfy-github/14-VERIFICATION.md"
    - "Every shipped Wave A Phase 14 adapter (Webhook, Ntfy, GitHub) has at least one observable-truth row"
    - "Every truth/claim cites a path:line reference that resolves against the current tree in internal/watcher/"
    - "The WebhookAdapter Setup/Listen/Teardown/HealthCheck lifecycle is cited against internal/watcher/webhook.go"
    - "The NtfyAdapter exponential backoff (2s initial, 2x factor, 30s cap) is cited with precise line anchors in internal/watcher/ntfy.go"
    - "The GitHubAdapter HMAC-SHA256 constant-time verification is cited with a path:line anchor at the hmac.Equal call in internal/watcher/github.go"
    - "The integration test wiring all three adapters through the engine (dedup + routing) is cited against internal/watcher/adapters_integration_test.go"
    - "The 62-test pass claim is reproduced by re-running go test ./internal/watcher/... -race -count=1 -timeout 120s and the output's final pass banner is pasted verbatim into the doc"
    - "No speculation words appear anywhere in 14-VERIFICATION.md: grep -E \"TODO|might|probably|likely\" returns zero matches"
  artifacts:
    - path: ".planning/phases/14-simple-adapters-webhook-ntfy-github/14-VERIFICATION.md"
      provides: "Observable-truth verification ledger entry for Phase 14 (webhook + ntfy + GitHub adapters) closing REQ-WF-1"
      min_lines: 80
      contains: "Observable Truths"
  key_links:
    - from: ".planning/phases/14-simple-adapters-webhook-ntfy-github/14-VERIFICATION.md"
      to: "internal/watcher/webhook.go"
      via: "path:line citation anchoring Setup / ServeHTTP / Listen"
      pattern: "internal/watcher/webhook\\.go:"
    - from: ".planning/phases/14-simple-adapters-webhook-ntfy-github/14-VERIFICATION.md"
      to: "internal/watcher/ntfy.go"
      via: "path:line citation anchoring the backoff reconnect loop"
      pattern: "internal/watcher/ntfy\\.go:"
    - from: ".planning/phases/14-simple-adapters-webhook-ntfy-github/14-VERIFICATION.md"
      to: "internal/watcher/github.go"
      via: "path:line citation anchoring hmac.Equal"
      pattern: "internal/watcher/github\\.go:.*hmac\\.Equal"
    - from: ".planning/phases/14-simple-adapters-webhook-ntfy-github/14-VERIFICATION.md"
      to: "internal/watcher/adapters_integration_test.go"
      via: "path:line citation to the three-adapter pipeline test"
      pattern: "internal/watcher/adapters_integration_test\\.go:"
    - from: ".planning/phases/14-simple-adapters-webhook-ntfy-github/14-VERIFICATION.md"
      to: "test runner output"
      via: "reproduced pass banner pasted verbatim"
      pattern: "ok\\s+.*agent-deck/internal/watcher"
---

<objective>
Backfill the Phase 14 verification ledger (`14-VERIFICATION.md`) for REQ-WF-1 by
producing an observable-truth report for the three shipped Wave A adapters
(Webhook, Ntfy, GitHub) with `path:line` citations against shipped source code
and test files, then reproducing the 62-test pass claim with a live
`go test ./internal/watcher/... -race -count=1 -timeout 120s` run.

Purpose: Close the v1.6.0 verification ledger lag for Phase 14 (REQ-WF-1). The
code shipped 2026-04-10; the verification doc was never written. This plan
produces the grep-able, citation-backed truth ledger that unblocks Wave B
Phases 20 and 21 (which depend on the Phase 14/15 surface being locked).

Output: A single new markdown file at
`.planning/phases/14-simple-adapters-webhook-ntfy-github/14-VERIFICATION.md`
following the GSD verification template used by `13-VERIFICATION.md`.

No code changes. This is a pure docs-backfill plan.
</objective>

<execution_context>
@/home/ashesh-goplani/agent-deck/.worktrees/watcher-completion/.claude/get-shit-done/workflows/execute-plan.md
@/home/ashesh-goplani/agent-deck/.worktrees/watcher-completion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-watcher-engine-core/13-VERIFICATION.md
@.planning/phases/14-simple-adapters-webhook-ntfy-github/14-01-SUMMARY.md
@.planning/phases/14-simple-adapters-webhook-ntfy-github/14-02-SUMMARY.md
@docs/WATCHER-COMPLETION-SPEC.md

# Shipped code to cite (executor MUST read these and derive exact line numbers
# from current source — do not trust numbers from prior SUMMARYs, which may
# have drifted if any file was touched after the summary was written):
@internal/watcher/webhook.go
@internal/watcher/webhook_test.go
@internal/watcher/ntfy.go
@internal/watcher/ntfy_test.go
@internal/watcher/github.go
@internal/watcher/github_test.go
@internal/watcher/adapters_integration_test.go
@internal/watcher/adapter.go

<format_reference>
The existing `.planning/phases/13-watcher-engine-core/13-VERIFICATION.md` is the
canonical GSD verification-doc format. Match its structure:

- Frontmatter: `phase`, `verified` (ISO8601 UTC), `status: passed`, `score: N/N`, `overrides_applied: 0`
- H1 title: "Phase 14: Simple Adapters (Webhook + ntfy + GitHub) Verification Report"
- "Phase Goal" one-liner, "Verified" timestamp, "Re-verification: No, initial verification"
- "## Goal Achievement" → "### Observable Truths" → markdown table with columns
  `| # | Truth | Status | Evidence |` where every Evidence cell contains at
  least one `path:line` citation
- "### Required Artifacts" table with `path | Expected | Status | Details`
- "### Key Link Verification" table wiring each claim to its source anchor
- "### Behavioral Spot-Checks" table with the `go test` command and the final
  pass banner as PASS evidence
- "### Requirements Coverage" table listing ADAPT-01, ADAPT-02, ADAPT-03 as SATISFIED
  with citation-backed evidence
</format_reference>

<interfaces>
<!-- Key code contracts the executor must cite. Derive EXACT line numbers from
     the shipped source in this worktree; do not trust any numbers in summaries. -->

From internal/watcher/adapter.go (canonical WatcherAdapter interface — all three
Phase 14 adapters implement this):
```go
type WatcherAdapter interface {
    Setup(ctx context.Context, cfg AdapterConfig) error
    Listen(ctx context.Context, events chan<- Event) error
    Teardown(ctx context.Context) error
    HealthCheck(ctx context.Context) error
}
```

From internal/watcher/webhook.go (cite the Setup / ServeHTTP / Listen / Teardown
/ HealthCheck method receivers and the net.Listen port-0 resolution path):
- `func (a *WebhookAdapter) Setup(...)` — HTTP mux + server construction
- `func (a *WebhookAdapter) Listen(...)` — net.Listen + http.Serve under RWMutex
- `func (a *WebhookAdapter) ServeHTTP(...)` — POST normalization, 202 response,
  1MB body limit, non-blocking channel send
- `func (a *WebhookAdapter) HealthCheck(...)` — TCP dial probe

From internal/watcher/ntfy.go (cite the backoff fields and the reconnect loop):
- Struct fields `initialBackoff` (2s default) / `maxBackoff` (30s default) /
  backoff multiplier (2x)
- `func (a *NtfyAdapter) streamOnce(...)` — NDJSON line reader via bufio.Scanner,
  skip `open` / `keepalive`, normalize message events
- `func (a *NtfyAdapter) Listen(...)` — wraps streamOnce in a backoff reconnect
  loop; cite the `time.After(backoff)` / `backoff *= 2` / `if backoff > maxBackoff`
  lines precisely

From internal/watcher/github.go (cite the HMAC path):
- `func verifySignature(...)` or equivalent — MUST anchor on the
  `hmac.Equal(computedMAC, providedMAC)` line (constant-time comparison)
- The caller in `ServeHTTP` that returns 401 on missing / malformed / invalid
  signatures before any body parsing
- Event normalization switch on `X-GitHub-Event` header: issues / pull_request /
  push / default (unknown)

From internal/watcher/adapters_integration_test.go (cite the three-adapter
pipeline test):
- `func TestWatcherAdapters_Integration...` (or the equivalent test name present
  in the current tree) — wires WebhookAdapter, NtfyAdapter (with a mock ntfy
  server), and GitHubAdapter through a real Engine with a temp statedb, asserts
  event persistence, routing, and EventCh delivery
- The dedup-across-adapters assertion confirming DedupKey incorporates Source
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive current path:line anchors from shipped Phase 14 code</name>
  <files>internal/watcher/webhook.go, internal/watcher/webhook_test.go, internal/watcher/ntfy.go, internal/watcher/ntfy_test.go, internal/watcher/github.go, internal/watcher/github_test.go, internal/watcher/adapters_integration_test.go</files>
  <read_first>
    - internal/watcher/webhook.go (entire file — map Setup, Listen, ServeHTTP, Teardown, HealthCheck methods to line numbers)
    - internal/watcher/webhook_test.go (entire file — note names of tests covering HTTP POST receipt, 1MB body cap, non-blocking send, health, goleak)
    - internal/watcher/ntfy.go (entire file — map NewNtfyAdapter field defaults, streamOnce, Listen reconnect loop, and the backoff arithmetic to line numbers)
    - internal/watcher/ntfy_test.go (entire file — note the test that asserts the 2s/2x/30s progression)
    - internal/watcher/github.go (entire file — map verifySignature, ServeHTTP 401 returns, and event normalization switch to line numbers)
    - internal/watcher/github_test.go (entire file — note the test that exercises tampered and valid HMAC payloads; note the constant-time / timing-attack test if present)
    - internal/watcher/adapters_integration_test.go (entire file — note the integration test function name(s) and the routing/dedup assertions)
    - internal/watcher/adapter.go (to cite the WatcherAdapter interface that each adapter implements)
    - .planning/phases/13-watcher-engine-core/13-VERIFICATION.md (format reference — do NOT copy content, only the structure)
    - .planning/phases/14-simple-adapters-webhook-ntfy-github/14-01-SUMMARY.md (source of human-written narrative; cite its claims against current code)
    - .planning/phases/14-simple-adapters-webhook-ntfy-github/14-02-SUMMARY.md (same)
  </read_first>
  <action>
    Scan each shipped file above and record the exact current line numbers for
    every anchor we plan to cite in 14-VERIFICATION.md. Do NOT trust the line
    numbers quoted in the SUMMARYs — files may have been touched after
    2026-04-10. Record these anchors in a scratch buffer (you may use a
    throwaway TODO list in your working memory; do NOT commit a scratch file).

    Required anchors to resolve (identifier → current path:line):

    WEBHOOK (internal/watcher/webhook.go):
    - `type WebhookAdapter struct { ... }` declaration
    - `func NewWebhookAdapter(...)` constructor
    - `func (a *WebhookAdapter) Setup(...)` method
    - `func (a *WebhookAdapter) Listen(...)` method — cite the `net.Listen` call
      and the mutex-protected `a.addr` write
    - `func (a *WebhookAdapter) ServeHTTP(...)` method — cite (a) the 1MB body
      cap (http.MaxBytesReader or similar), (b) the `w.WriteHeader(202)` call,
      (c) the non-blocking `select { case a.eventsCh <- e: default: }` send
    - `func (a *WebhookAdapter) Teardown(...)` method
    - `func (a *WebhookAdapter) HealthCheck(...)` method — cite the net.Dial
      probe
    In internal/watcher/webhook_test.go:
    - The test that exercises POST → 202 → event on channel
    - The test that covers the 1MB body limit
    - The test that calls goleak.VerifyNone at teardown

    NTFY (internal/watcher/ntfy.go):
    - `type NtfyAdapter struct { ... }` declaration — cite the
      `initialBackoff` / `maxBackoff` field declarations (default values)
    - `func NewNtfyAdapter(...)` constructor setting 2s / 30s defaults
    - `func (a *NtfyAdapter) streamOnce(...)` — cite the NDJSON
      bufio.Scanner loop and the `open` / `keepalive` skip
    - `func (a *NtfyAdapter) Listen(...)` — cite (a) the `time.After(backoff)`
      sleep, (b) `backoff *= 2` multiplier, (c) `if backoff > a.maxBackoff { backoff = a.maxBackoff }` cap
    - `func (a *NtfyAdapter) HealthCheck(...)`
    In internal/watcher/ntfy_test.go:
    - The test asserting reconnect progression from 2s → 4s → 8s → … → capped
      at 30s (the test will use small override values, but the ARITHMETIC is
      the claim — cite both the test name and the lines that set the override
      field values)
    - The open/keepalive-skip test
    - The goleak test

    GITHUB (internal/watcher/github.go):
    - `type GitHubAdapter struct { ... }` declaration
    - `func NewGitHubAdapter(...)` constructor — default bind 127.0.0.1:18461
    - `func (a *GitHubAdapter) verifySignature(...)` OR the equivalent function
      — THE LINE THAT CALLS `hmac.Equal(...)` (this is the central REQ-WF-1
      claim: constant-time comparison preventing timing attacks)
    - `func (a *GitHubAdapter) ServeHTTP(...)` — cite (a) the signature
      header read (X-Hub-Signature-256), (b) the 401 response on missing /
      malformed / invalid signatures BEFORE any body normalization, (c) the
      10MB body cap, (d) the X-GitHub-Event switch covering issues /
      pull_request / push / default
    - The push-event pusher-email-preferred-over-sender branch
    In internal/watcher/github_test.go:
    - The test(s) exercising a VALID signature → 202 + event path
    - The test(s) exercising INVALID / MISSING / MALFORMED signature → 401 +
      no event emission
    - The body-size-limit test
    - The four normalization tests (issues, pull_request, push, unknown)
    - The goleak test

    INTEGRATION (internal/watcher/adapters_integration_test.go):
    - The function name and line of the test that spins up all three adapters
      together
    - The assertion that 3 events are persisted in the temp statedb
    - The assertion that each event is routed to the correct conductor via
      clients.json exact and wildcard (*@github.com) rules
    - The assertion that EventCh delivers 3 events with the correct Source
      field (webhook / ntfy / github)
    - The follow-on dedup test confirming DedupKey incorporates Source (same
      sender + subject from two different adapters are NOT deduped)

    Output of this task: an in-memory anchor table you will feed into Task 2.
    No files are written in this task.
  </action>
  <verify>
    <automated>test -f internal/watcher/webhook.go && test -f internal/watcher/ntfy.go && test -f internal/watcher/github.go && test -f internal/watcher/adapters_integration_test.go</automated>
  </verify>
  <acceptance_criteria>
    - All four shipped source files and their test counterparts exist in the working tree (bash `test -f` passes for each).
    - You have derived a current-tree line number for every anchor listed in the action (executor self-attests in Task 2's doc output by citing them).
  </acceptance_criteria>
  <done>Executor has a complete anchor table in working memory with current-tree line numbers for every identifier listed above, ready to be rendered into 14-VERIFICATION.md in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Write 14-VERIFICATION.md with path:line citations per observable truth</name>
  <files>.planning/phases/14-simple-adapters-webhook-ntfy-github/14-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/13-watcher-engine-core/13-VERIFICATION.md (structural template — match its frontmatter, H2/H3 hierarchy, and table shapes)
    - .planning/phases/14-simple-adapters-webhook-ntfy-github/14-01-SUMMARY.md (narrative source for webhook+ntfy claims)
    - .planning/phases/14-simple-adapters-webhook-ntfy-github/14-02-SUMMARY.md (narrative source for github+integration claims)
    - docs/WATCHER-COMPLETION-SPEC.md §"REQ-WF-1" (canonical rule for this doc's content)
    - The anchor table derived in Task 1 (your working memory)
  </read_first>
  <action>
    Create `.planning/phases/14-simple-adapters-webhook-ntfy-github/14-VERIFICATION.md`
    with the exact structure below (match `13-VERIFICATION.md` stylistically).

    FRONTMATTER (YAML):
    ```
    ---
    phase: 14-simple-adapters-webhook-ntfy-github
    verified: <ISO8601 UTC timestamp at time of writing, e.g. 2026-04-16T00:00:00Z>
    status: passed
    score: <N/N, where N is the total number of observable truths you list below>
    overrides_applied: 0
    ---
    ```

    H1: `# Phase 14: Simple Adapters (Webhook + ntfy + GitHub) Verification Report`

    One-paragraph "Phase Goal" restatement (grounded in ADAPT-01/02/03 language
    from REQUIREMENTS.md lines 84–86), then "Verified:", "Status: passed",
    "Re-verification: No, initial verification".

    `## Goal Achievement` → `### Observable Truths` table with AT LEAST 8 rows:

    Row 1 — WebhookAdapter lifecycle: cite `internal/watcher/webhook.go:<Setup line>` Setup, `:<Listen line>` Listen with net.Listen + RWMutex addr write, `:<Teardown line>` Teardown, `:<HealthCheck line>` HealthCheck TCP dial, `:<ServeHTTP line>` POST handler returning 202.

    Row 2 — WebhookAdapter body-size + non-blocking send safety: cite the 1MB
    `http.MaxBytesReader` line and the `select { case … default: }` send line
    in webhook.go, plus the webhook_test.go lines for the body-limit test and
    the non-blocking-send test.

    Row 3 — NtfyAdapter NDJSON subscription: cite `internal/watcher/ntfy.go:<streamOnce line>` for the bufio.Scanner NDJSON loop and the open/keepalive skip branches.

    Row 4 — NtfyAdapter exponential backoff (2s initial / 2x factor / 30s cap)
    — THIS IS THE CENTRAL REQ-WF-1 ntfy CLAIM. Cite the three precise lines in
    ntfy.go: the `initialBackoff` default field, the `backoff *= 2`
    multiplication, and the `if backoff > a.maxBackoff { backoff = a.maxBackoff }`
    cap. Also cite the ntfy_test.go line for the reconnect-progression test.

    Row 5 — GitHubAdapter HMAC-SHA256 constant-time verification — THIS IS THE
    CENTRAL REQ-WF-1 github CLAIM. Cite
    `internal/watcher/github.go:<line>` where `hmac.Equal(...)` is called. Cite
    the test file line(s) that exercise BOTH valid and tampered signature
    payloads. Explicitly call out that `hmac.Equal` is constant-time per Go
    crypto/hmac docs (no speculation words).

    Row 6 — GitHubAdapter event normalization across 4 event types: cite the
    X-GitHub-Event switch lines for issues, pull_request, push, and default
    (unknown) in github.go. Cite the four corresponding test functions in
    github_test.go.

    Row 7 — Integration pipeline (all three adapters → engine → dedup →
    routing): cite the `TestWatcherAdapters_Integration*` function line in
    `internal/watcher/adapters_integration_test.go` plus the specific
    assertions (persistence count=3, routing to correct conductors, EventCh
    delivery by Source).

    Row 8 — Dedup across adapters: cite the integration test assertion that
    DedupKey incorporates Source (same sender+subject from two adapters are
    NOT deduped).

    `### Required Artifacts` table: one row per shipped file
    (webhook.go, webhook_test.go, ntfy.go, ntfy_test.go, github.go,
    github_test.go, adapters_integration_test.go, testmain_test.go), Status =
    VERIFIED, Details = line count + principal exports derived in Task 1.

    `### Key Link Verification` table: one row per (From → To → Via → Status)
    tuple — e.g. `webhook.go → AdapterConfig (adapter.go)` via Setup(cfg),
    `github.go → hmac.Equal (stdlib)` via verifySignature,
    `adapters_integration_test.go → engine.go` via Engine.Start + Register.

    `### Behavioral Spot-Checks` table: PENDING rows that Task 3 will fill
    with live `go test` output. Leave the "Result" cells as
    `<filled by Task 3>` placeholders — Task 3 will edit this doc in place to
    replace those placeholders with actual output.

    `### Requirements Coverage` table: three rows — ADAPT-01, ADAPT-02,
    ADAPT-03 — each marked SATISFIED with a one-line evidence summary pointing
    at the Observable Truth row that backs it.

    Closing footer: `---` then `*Phase: 14-simple-adapters-webhook-ntfy-github*`
    and `*Completed: <YYYY-MM-DD>*`.

    HARD PROHIBITIONS — the following substrings MUST NOT appear anywhere in
    the written doc: `TODO`, `might`, `probably`, `likely` (case-sensitive per
    ROADMAP success criterion #5). Also forbid "will be done", "to be
    determined", "placeholder" anywhere EXCEPT the explicit
    `<filled by Task 3>` placeholder strings in the Behavioral Spot-Checks
    table — those are removed by Task 3 and do not ship.

    Every Evidence cell in the Observable Truths table MUST contain at least
    one `path:line` token matching the regex
    `internal/watcher/[a-z_]+\.go:[0-9]+` OR
    `internal/watcher/[a-z_]+_test\.go:[0-9]+`.
  </action>
  <verify>
    <automated>test -f .planning/phases/14-simple-adapters-webhook-ntfy-github/14-VERIFICATION.md &amp;&amp; grep -Ec "internal/watcher/[a-z_]+\.go:[0-9]+" .planning/phases/14-simple-adapters-webhook-ntfy-github/14-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/14-simple-adapters-webhook-ntfy-github/14-VERIFICATION.md` exits 0.
    - `grep -Ec "internal/watcher/[a-z_]+\.go:[0-9]+" 14-VERIFICATION.md` returns a count >= 8 (one citation per observable-truth row minimum).
    - `grep -c "internal/watcher/webhook.go:" 14-VERIFICATION.md` >= 2 (lifecycle row + body-cap row).
    - `grep -c "internal/watcher/ntfy.go:" 14-VERIFICATION.md` >= 2 (NDJSON row + backoff row).
    - `grep -c "internal/watcher/github.go:" 14-VERIFICATION.md` >= 2 (HMAC row + normalization row).
    - `grep -c "hmac.Equal" 14-VERIFICATION.md` >= 1 (constant-time comparison is explicitly cited).
    - `grep -c "internal/watcher/adapters_integration_test.go:" 14-VERIFICATION.md` >= 1 (integration pipeline cited).
    - `grep -E "\bTODO\b|\bmight\b|\bprobably\b|\blikely\b" 14-VERIFICATION.md` returns NO matches (exit 1).
    - `grep -c "ADAPT-01\|ADAPT-02\|ADAPT-03" 14-VERIFICATION.md` >= 3 (all three requirements appear in Requirements Coverage).
    - `grep -c "Observable Truths" 14-VERIFICATION.md` >= 1 AND `grep -c "Required Artifacts" 14-VERIFICATION.md` >= 1 AND `grep -c "Key Link Verification" 14-VERIFICATION.md` >= 1 AND `grep -c "Requirements Coverage" 14-VERIFICATION.md` >= 1.
  </acceptance_criteria>
  <done>14-VERIFICATION.md exists with at least 8 observable-truth rows, each citing path:line in internal/watcher/*, covering all three adapters (webhook + ntfy + github) and the integration test, with zero speculation words.</done>
</task>

<task type="auto">
  <name>Task 3: Reproduce the 62-test pass claim and replace spot-check placeholders; final anti-speculation gate; commit</name>
  <files>.planning/phases/14-simple-adapters-webhook-ntfy-github/14-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/14-simple-adapters-webhook-ntfy-github/14-VERIFICATION.md (the file Task 2 just wrote — you're editing it in place)
    - CLAUDE.md at repo root (to re-confirm: no --no-verify on source commits; commit signature is "Committed by Ashesh Goplani"; no Claude attribution)
  </read_first>
  <action>
    Step 1: Run the full watcher test suite under the race detector with a
    fixed timeout:

      go test ./internal/watcher/... -race -count=1 -timeout 120s

    Capture the complete output. Extract (a) the final `ok .../internal/watcher ...`
    line (the pass banner), (b) the `PASS` lines per test file, and (c) the
    total test count. If any sub-package fails, STOP and return the failure
    to the orchestrator — do NOT paper over a test failure to close the
    verification doc.

    Step 2: Edit `14-VERIFICATION.md` in place: replace the
    `<filled by Task 3>` placeholders in the `### Behavioral Spot-Checks`
    table with the actual output captured in Step 1. The table MUST end up
    with at least these rows:

    | Behavior | Command | Result | Status |
    |----------|---------|--------|--------|
    | All Phase 14 watcher tests pass with race detector | `go test ./internal/watcher/... -race -count=1 -timeout 120s` | <pasted pass banner, e.g. `ok github.com/ashgoplani/agent-deck/internal/watcher 4.123s`> | PASS |
    | Test count matches 62-test claim from SUMMARYs | `go test ./internal/watcher/... -race -count=1 -v 2>&amp;1 \| grep -c "^--- PASS"` | <pasted integer; if it differs from 62, quote the actual number and note "tests added post-Phase-14 are counted; the Phase-14 suite still passes"> | PASS |
    | Package builds cleanly | `go build ./internal/watcher/...` | Exit 0 | PASS |
    | No vet warnings | `go vet ./internal/watcher/...` | Exit 0 | PASS |

    Note the "tests added post-Phase-14" branch is ONLY for the case where
    current test count exceeds 62 because Phase 15/17/18 added tests to the
    same package. If the current count is LESS than 62, that is a regression
    and you must surface it as a verification failure.

    Step 3: Run the final anti-speculation grep gate:

      grep -E "\bTODO\b|\bmight\b|\bprobably\b|\blikely\b" .planning/phases/14-simple-adapters-webhook-ntfy-github/14-VERIFICATION.md

    If anything matches, edit the doc to eliminate the speculation word
    (replace with a concrete citation or remove the sentence). Re-run until
    the grep returns no matches.

    Step 4: Confirm the Behavioral Spot-Checks placeholder is gone:

      grep -c "<filled by Task 3>" .planning/phases/14-simple-adapters-webhook-ntfy-github/14-VERIFICATION.md

    Must return 0.

    Step 5: Commit the doc with a signed message. Per the repo-root CLAUDE.md
    mandate: source-code commit hooks must pass; this is a `.planning/`-only
    commit but we still honor the signature rule and do NOT use --no-verify.
    Commit message format:

      docs(19-01): Phase 14 verification doc — webhook + ntfy + GitHub adapters

      Closes REQ-WF-1. Observable-truth ledger for shipped Wave A adapters
      with path:line citations against internal/watcher/{webhook,ntfy,github}.go
      and adapters_integration_test.go. 62-test pass claim reproduced via
      go test ./internal/watcher/... -race -count=1 -timeout 120s.

      Committed by Ashesh Goplani

    Stage ONLY the verification doc. Do not stage other files.
  </action>
  <verify>
    <automated>go test ./internal/watcher/... -race -count=1 -timeout 120s &amp;&amp; ! grep -qE "\bTODO\b|\bmight\b|\bprobably\b|\blikely\b" .planning/phases/14-simple-adapters-webhook-ntfy-github/14-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/watcher/... -race -count=1 -timeout 120s` exits 0 on the current tree (this is ROADMAP success criterion #4).
    - `grep "<filled by Task 3>" .planning/phases/14-simple-adapters-webhook-ntfy-github/14-VERIFICATION.md` returns zero matches.
    - `grep -E "\bTODO\b|\bmight\b|\bprobably\b|\blikely\b" .planning/phases/14-simple-adapters-webhook-ntfy-github/14-VERIFICATION.md` returns zero matches (ROADMAP success criterion #5, Phase 14 portion).
    - `grep -c "ok.*internal/watcher" .planning/phases/14-simple-adapters-webhook-ntfy-github/14-VERIFICATION.md` >= 1 (pass banner pasted).
    - `git log -1 --pretty=%B` contains `Committed by Ashesh Goplani`.
    - `git log -1 --pretty=%B | grep -ciE "claude|co-authored-by"` returns 0 (no Claude attribution).
    - `git log -1 --name-only` shows ONLY `.planning/phases/14-simple-adapters-webhook-ntfy-github/14-VERIFICATION.md`.
    - No `--no-verify` flag was passed to git commit (executor self-attests; hooks must no-op cleanly on a .planning-only commit).
  </acceptance_criteria>
  <done>Verification doc is committed, test suite passes cleanly, anti-speculation grep returns zero matches, and the commit is signed "Committed by Ashesh Goplani".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — documentation-only phase, no code paths modified, no new data flows
introduced. The verification doc is read-only text consumed by humans and
`grep`.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | N/A | 14-VERIFICATION.md (Markdown docs) | not-applicable | Docs-only; no attack surface. ASVS L1 controls (auth, session mgmt, input validation, crypto) are all not-applicable. |
</threat_model>

<verification>
Phase-level success criteria for plan 19-01 (map to ROADMAP Phase 19 success
criteria #1, #2, #4, and the Phase 14 portion of #5):

1. `.planning/phases/14-simple-adapters-webhook-ntfy-github/14-VERIFICATION.md`
   exists with at least one observable-truth row per shipped adapter
   (Webhook, Ntfy, GitHub) and at least one row for the integration test.
2. Every claim in 14-VERIFICATION.md cites a `path:line` reference of the form
   `internal/watcher/<file>.go:<N>` that resolves in the current tree.
3. Rerunning `go test ./internal/watcher/... -race -count=1 -timeout 120s`
   exits 0 and the final pass banner is pasted verbatim into the Behavioral
   Spot-Checks table.
4. `grep -E "\bTODO\b|\bmight\b|\bprobably\b|\blikely\b" .planning/phases/14-*/14-VERIFICATION.md`
   returns zero matches.
5. The file is committed on `feat/watcher-completion` with the signature
   `Committed by Ashesh Goplani` and no Claude attribution.
</verification>

<success_criteria>
- `test -f .planning/phases/14-simple-adapters-webhook-ntfy-github/14-VERIFICATION.md` → exit 0
- `grep -c "Observable Truths" 14-VERIFICATION.md` >= 1
- `grep -Ec "internal/watcher/[a-z_]+\.go:[0-9]+" 14-VERIFICATION.md` >= 8
- `grep -c "hmac.Equal" 14-VERIFICATION.md` >= 1
- `grep -E "2s|initialBackoff" 14-VERIFICATION.md | grep -c "ntfy"` >= 1 (backoff timings documented with ntfy context)
- `grep -E "\bTODO\b|\bmight\b|\bprobably\b|\blikely\b" 14-VERIFICATION.md` returns no matches
- `go test ./internal/watcher/... -race -count=1 -timeout 120s` exits 0
- `git log -1 --pretty=%B | grep -c "Committed by Ashesh Goplani"` == 1
- `git log -1 --pretty=%B | grep -ciE "claude|co-authored-by"` == 0
- REQ-WF-1 status in REQUIREMENTS.md can be flipped to `[x]` by a downstream maintainer (not this plan's job to flip it)
</success_criteria>

<output>
After completion, create
`.planning/phases/19-verification-docs-phases-14-15/19-01-SUMMARY.md` using the
standard GSD summary template. The SUMMARY must include:

- Frontmatter with `requirements-completed: [REQ-WF-1]`
- Metrics: duration, files modified (just 14-VERIFICATION.md)
- Task-by-task commit SHAs (one commit total for this plan)
- "Evidence reproduced" section quoting the final `go test` pass banner
- "Next phase readiness" note that REQ-WF-1 is closed and Phase 19 plan 19-02
  (Phase 15 backfill) can proceed in the same wave (they're already siblings
  in wave 1)
</output>
