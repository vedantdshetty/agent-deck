---
phase: 19
plan: 19-02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/15-slack-adapter-and-import/15-01-PLAN.md
  - .planning/phases/15-slack-adapter-and-import/15-01-SUMMARY.md
  - .planning/phases/15-slack-adapter-and-import/15-VERIFICATION.md
autonomous: true
requirements:
  - REQ-WF-2
tags: [docs-backfill, verification, watcher, slack, cli, watcher-import]
user_setup: []

must_haves:
  truths:
    - "15-01-PLAN.md exists at .planning/phases/15-slack-adapter-and-import/15-01-PLAN.md reconstructed from shipped slack.go + tests"
    - "15-01-SUMMARY.md exists at .planning/phases/15-slack-adapter-and-import/15-01-SUMMARY.md following the GSD summary template"
    - "15-VERIFICATION.md exists at .planning/phases/15-slack-adapter-and-import/15-VERIFICATION.md with observable-truth rows"
    - "Every claim in 15-VERIFICATION.md cites a path:line reference that resolves against the current tree"
    - "The Slack adapter WatcherAdapter interface implementation (Setup / Listen / streamOnce / normalizeV1 / normalizeV2 / Teardown / HealthCheck) is cited against internal/watcher/slack.go"
    - "The slack:{CHANNEL_ID} routing key format is cited to the line in internal/watcher/slack.go where the Sender field is set with fmt.Sprintf(\"slack:%s\", ...)"
    - "The watcher import atomic merge via os.Rename is cited to the line in cmd/agent-deck/watcher_cmd.go"
    - "The Lstat-based symlink rejection is cited to the line in cmd/agent-deck/watcher_cmd.go where os.Lstat is called and the \"symlink not allowed\" error is returned"
    - "No speculation: grep -E \"TODO|might|probably|likely\" across 15-01-PLAN.md, 15-01-SUMMARY.md, 15-VERIFICATION.md returns zero matches"
  artifacts:
    - path: ".planning/phases/15-slack-adapter-and-import/15-01-PLAN.md"
      provides: "Reconstructed plan-of-record for shipped Phase 15 Wave 1 Slack adapter implementation (ADAPT-04)"
      min_lines: 60
      contains: "SlackAdapter"
    - path: ".planning/phases/15-slack-adapter-and-import/15-01-SUMMARY.md"
      provides: "Reconstructed summary capturing what was shipped in Phase 15 Wave 1 (Slack adapter)"
      min_lines: 50
      contains: "requirements-completed"
    - path: ".planning/phases/15-slack-adapter-and-import/15-VERIFICATION.md"
      provides: "Observable-truth verification ledger for Phase 15 (Slack adapter + watcher import) closing REQ-WF-2"
      min_lines: 70
      contains: "Observable Truths"
  key_links:
    - from: ".planning/phases/15-slack-adapter-and-import/15-VERIFICATION.md"
      to: "internal/watcher/slack.go"
      via: "path:line citation anchoring SlackAdapter lifecycle methods"
      pattern: "internal/watcher/slack\\.go:"
    - from: ".planning/phases/15-slack-adapter-and-import/15-VERIFICATION.md"
      to: "internal/watcher/slack.go"
      via: "path:line citation to slack:{CHANNEL_ID} Sender assignment"
      pattern: "slack:%s|slack:.*CHANNEL"
    - from: ".planning/phases/15-slack-adapter-and-import/15-VERIFICATION.md"
      to: "cmd/agent-deck/watcher_cmd.go"
      via: "path:line citation to os.Lstat symlink rejection"
      pattern: "cmd/agent-deck/watcher_cmd\\.go:"
    - from: ".planning/phases/15-slack-adapter-and-import/15-VERIFICATION.md"
      to: "cmd/agent-deck/watcher_cmd.go"
      via: "path:line citation to os.Rename atomic write"
      pattern: "os\\.Rename"
    - from: ".planning/phases/15-slack-adapter-and-import/15-VERIFICATION.md"
      to: "internal/watcher/slack_test.go"
      via: "path:line citation to Slack adapter tests"
      pattern: "internal/watcher/slack_test\\.go:"
---

<objective>
Backfill the Phase 15 (Slack adapter + `watcher import`) documentation for
REQ-WF-2. Three files are produced from shipped code evidence:

1. `15-01-PLAN.md` — a reconstructed plan-of-record covering the Slack adapter
   Wave 1 work that shipped but never had a PLAN written.
2. `15-01-SUMMARY.md` — a reconstructed summary of that Wave 1 work matching
   the template used by `15-02-SUMMARY.md` (already present in the tree for
   the watcher-import work).
3. `15-VERIFICATION.md` — the observable-truth ledger covering BOTH Wave 1
   (Slack adapter, ADAPT-04) and Wave 2 (watcher import, CLI-07), with
   `path:line` citations for every claim.

Purpose: Close the v1.6.0 verification ledger lag for Phase 15 (REQ-WF-2). The
code shipped 2026-04-10 (`internal/watcher/slack.go` ~450 lines + `watcher
import` in `cmd/agent-deck/watcher_cmd.go`). Wave 2's SUMMARY exists; Wave 1
was never documented and the phase-level verification doc was never written.
This plan produces all three docs in a single commit.

Output:
- `.planning/phases/15-slack-adapter-and-import/15-01-PLAN.md`
- `.planning/phases/15-slack-adapter-and-import/15-01-SUMMARY.md`
- `.planning/phases/15-slack-adapter-and-import/15-VERIFICATION.md`

No code changes.
</objective>

<execution_context>
@/home/ashesh-goplani/agent-deck/.worktrees/watcher-completion/.claude/get-shit-done/workflows/execute-plan.md
@/home/ashesh-goplani/agent-deck/.worktrees/watcher-completion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-watcher-engine-core/13-VERIFICATION.md
@.planning/phases/15-slack-adapter-and-import/15-02-SUMMARY.md
@docs/WATCHER-COMPLETION-SPEC.md

# Shipped code to cite (executor MUST read these and derive exact line numbers
# from current source — do not trust the line numbers quoted in 15-02-SUMMARY.md,
# they may have drifted since that doc was written):
@internal/watcher/slack.go
@internal/watcher/slack_test.go
@internal/watcher/adapter.go
@cmd/agent-deck/watcher_cmd.go
@cmd/agent-deck/watcher_cmd_test.go

<format_reference>
- `13-VERIFICATION.md` is the canonical GSD verification-doc template (match
  its frontmatter, H2/H3 hierarchy, Observable Truths / Required Artifacts /
  Key Link Verification / Behavioral Spot-Checks / Requirements Coverage
  tables).
- `15-02-SUMMARY.md` is the canonical GSD summary template used by this very
  phase (copy its frontmatter shape and section headers for 15-01-SUMMARY.md).
- GSD PLAN template: match the frontmatter and task-list shape used throughout
  `.planning/phases/*/NN-NN-PLAN.md` files. For a backfill PLAN the executor
  may note explicitly in the objective that "this PLAN is reconstructed
  retrospectively from shipped code as part of REQ-WF-2 backfill; the
  original task sequence is inferred from commits in git log".
</format_reference>

<interfaces>
<!-- Key code contracts the executor must cite. Approximate current-tree line
     numbers based on grep results (as of planning time); executor MUST
     re-verify each anchor by reading the live file at execute time. -->

From internal/watcher/slack.go (SlackAdapter implementing WatcherAdapter):
- `func (a *SlackAdapter) Setup(...)` near line 57 — adapter configuration from ntfy bridge URL
- `func (a *SlackAdapter) Listen(...)` near line 86 — wraps streamOnce in a reconnect loop
- `func (a *SlackAdapter) streamOnce(...)` near line 109 — NDJSON stream from the ntfy bridge (shared with NtfyAdapter pattern)
- `func (a *SlackAdapter) normalizeSlackEvent(...)` near line 164 — event shape selector (v1 vs v2)
- `func (a *SlackAdapter) normalizeV2(...)` near line 173 — THE LOCUS of `slack:{CHANNEL_ID}` Sender formatting; look for `Sender: fmt.Sprintf("slack:%s", payload.Channel)` around line 181
- `func (a *SlackAdapter) normalizeV1(...)` near line 198 — v1 legacy format handler; `slack:unknown` fallback Sender near line 206
- `func (a *SlackAdapter) Teardown()` near line 216
- `func (a *SlackAdapter) HealthCheck()` near line 222

From internal/watcher/slack_test.go: enumerate the tests covering v1 parse,
v2 parse, channel-routing Sender format, reconnect, teardown, and any
goleak-bounded test.

From cmd/agent-deck/watcher_cmd.go (watcher import implementation, per
15-02-SUMMARY.md frontmatter key-decisions):
- `os.Rename(tmpName, clientsPath)` atomic write near line 708 — THE atomic-merge citation
- `info, err := os.Lstat(cleanPath)` near line 723 — THE symlink rejection citation
- `return fmt.Errorf("symlink not allowed: %q", cleanPath)` near line 728 — the rejection branch
- `clientKey := fmt.Sprintf("slack:%s", channelID)` near line 772 — the
  slack:{CHANNEL_ID} key in the import path (mirrors the same format used by
  SlackAdapter's Sender field)

From cmd/agent-deck/watcher_cmd_test.go: enumerate the 13 tests listed in
15-02-SUMMARY.md (parse valid/invalid/empty/nonexistent, generate TOML, merge
new/existing/overwrite, end-to-end, idempotent, symlink rejection, directory
rejection, empty channels).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive current path:line anchors from shipped Phase 15 code</name>
  <files>internal/watcher/slack.go, internal/watcher/slack_test.go, cmd/agent-deck/watcher_cmd.go, cmd/agent-deck/watcher_cmd_test.go</files>
  <read_first>
    - internal/watcher/slack.go (entire file — map every SlackAdapter method + the two normalize variants to current line numbers; note the exact line where `slack:%s` Sender is set)
    - internal/watcher/slack_test.go (entire file — enumerate tests and note line numbers for: the v2 channel-routing Sender test, the v1 legacy format test, the reconnect test, the teardown/goleak test)
    - cmd/agent-deck/watcher_cmd.go — specifically: os.Lstat call + "symlink not allowed" error return, os.Rename atomic write, fmt.Sprintf("slack:%s", channelID) in the import merge path, handleWatcher dispatch, handleWatcherImport function
    - cmd/agent-deck/watcher_cmd_test.go (entire file — enumerate the 13 tests from 15-02-SUMMARY.md; for each, note its line number and the specific assertion it makes)
    - .planning/phases/15-slack-adapter-and-import/15-02-SUMMARY.md (canonical reference for watcher import narrative; cite its decisions against current code)
    - .planning/phases/13-watcher-engine-core/13-VERIFICATION.md (format template)
    - docs/WATCHER-COMPLETION-SPEC.md §REQ-WF-2 (canonical rule for this doc set's content)
    - git log --oneline internal/watcher/slack.go internal/watcher/slack_test.go (to reconstruct the Wave 1 commit timeline for 15-01-PLAN.md and 15-01-SUMMARY.md — capture commit SHAs, author, dates, and subject lines)
  </read_first>
  <action>
    Scan each shipped file and record exact current-tree line numbers for
    every anchor needed across the three docs to be produced in Tasks 2–4. Do
    NOT trust line numbers in 15-02-SUMMARY.md — re-derive from the current
    tree.

    Required anchors (identifier → current path:line):

    SLACK ADAPTER (internal/watcher/slack.go):
    - `type SlackAdapter struct { ... }` declaration
    - `func NewSlackAdapter(...)` constructor if present; otherwise the
      equivalent initialization function
    - `func (a *SlackAdapter) Setup(...)` — config validation
    - `func (a *SlackAdapter) Listen(...)` — backoff reconnect wrapper
    - `func (a *SlackAdapter) streamOnce(...)` — NDJSON stream reader against
      the ntfy bridge; this is the Slack-routes-via-ntfy-bridge claim from
      ADAPT-04
    - `func (a *SlackAdapter) normalizeSlackEvent(...)` — v1-vs-v2 dispatcher
    - `func (a *SlackAdapter) normalizeV2(...)` — THE CENTRAL
      slack:{CHANNEL_ID} claim; cite the exact line containing
      `Sender: fmt.Sprintf("slack:%s", payload.Channel)` or equivalent
    - `func (a *SlackAdapter) normalizeV1(...)` — legacy path + the
      `slack:unknown` fallback Sender line
    - `func (a *SlackAdapter) Teardown()`
    - `func (a *SlackAdapter) HealthCheck()`

    SLACK TESTS (internal/watcher/slack_test.go):
    - The test that posts a v2 channel-routed message and asserts the emitted
      Event's Sender == "slack:<CHANNEL_ID>"
    - The test that parses a v1 legacy Slack payload and produces the
      "slack:unknown" fallback Sender
    - The test covering stream reconnect
    - The goleak or teardown test

    WATCHER IMPORT (cmd/agent-deck/watcher_cmd.go):
    - The line containing `os.Lstat(cleanPath)` (~723)
    - The line returning `fmt.Errorf("symlink not allowed: %q", cleanPath)`
      (~728) — cite BOTH of these together; they form the REQ-WF-2 symlink
      rejection claim
    - The directory-rejection branch adjacent to the symlink check (if
      present; documented in 15-02-SUMMARY.md line mentioning "directory
      rejection" test)
    - The `os.Rename(tmpName, clientsPath)` atomic-merge line (~708) — THE
      REQ-WF-2 atomic-merge claim
    - The `fmt.Sprintf("slack:%s", channelID)` line (~772) — THE REQ-WF-2
      slack:{CHANNEL_ID} key format claim in the import path
    - `handleWatcher(...)` dispatch function
    - `handleWatcherImport(...)` entry point

    WATCHER IMPORT TESTS (cmd/agent-deck/watcher_cmd_test.go):
    - Enumerate all 13 tests listed in 15-02-SUMMARY.md "Files Created/Modified"
      and record their line numbers, paying specific attention to:
      * symlink rejection test
      * directory rejection test
      * end-to-end test
      * idempotent test (re-running import produces same output)
      * merge new/existing/overwrite tests

    GIT HISTORY (for reconstructing 15-01-PLAN.md and 15-01-SUMMARY.md):
    - Find the Wave 1 Slack adapter commits with:
        git log --oneline --all -- internal/watcher/slack.go internal/watcher/slack_test.go
      Record the commit SHAs, subject lines, dates, and whether they follow
      the test(15-01)/feat(15-01) TDD pair pattern from `15-02-SUMMARY.md`.
      These SHAs go into the "Task Commits" section of 15-01-SUMMARY.md.

    Output: an in-memory anchor table + commit list ready to be rendered into
    Tasks 2, 3, and 4. No files are written in this task.
  </action>
  <verify>
    <automated>test -f internal/watcher/slack.go &amp;&amp; test -f internal/watcher/slack_test.go &amp;&amp; test -f cmd/agent-deck/watcher_cmd.go &amp;&amp; test -f cmd/agent-deck/watcher_cmd_test.go</automated>
  </verify>
  <acceptance_criteria>
    - All four shipped source files exist in the working tree (bash `test -f` passes for each).
    - Executor has derived current-tree line numbers for every anchor listed in the action.
    - Executor has enumerated the Wave 1 Slack adapter commits from git log to backfill 15-01-SUMMARY.md.
  </acceptance_criteria>
  <done>Anchor table and commit history are in working memory, ready to drive Tasks 2–4.</done>
</task>

<task type="auto">
  <name>Task 2: Write 15-01-PLAN.md (reconstructed Slack adapter plan-of-record)</name>
  <files>.planning/phases/15-slack-adapter-and-import/15-01-PLAN.md</files>
  <read_first>
    - .planning/phases/14-simple-adapters-webhook-ntfy-github/14-01-SUMMARY.md (references its upstream plan — use as a structural template for the look of a typical PLAN in this project; also see any 14-01-PLAN.md if present to mirror frontmatter fields)
    - .planning/phases/15-slack-adapter-and-import/15-02-SUMMARY.md (the Wave 2 summary gives us the frontmatter shape used in this phase)
    - Anchor table from Task 1
  </read_first>
  <action>
    Create `.planning/phases/15-slack-adapter-and-import/15-01-PLAN.md` as a
    reconstructed plan-of-record. Make it explicit in the objective that this
    PLAN is a retrospective reconstruction:

    FRONTMATTER (YAML):
    ```
    ---
    phase: 15
    plan: 15-01
    type: execute
    wave: 1
    depends_on: []
    files_modified:
      - internal/watcher/slack.go
      - internal/watcher/slack_test.go
    autonomous: true
    requirements:
      - ADAPT-04
    backfill: true   # Flag indicating this PLAN was reconstructed after the code shipped (REQ-WF-2)
    ---
    ```

    BODY:

    `<objective>` section explicitly stating:
    "This PLAN is a retrospective reconstruction of the Phase 15 Wave 1 work
    (SlackAdapter implementation) that shipped on <date from git log> in
    commit(s) <SHAs from Task 1>. The plan is written after the fact as part
    of REQ-WF-2 backfill; the task sequence is inferred from the commit
    history and file structure."

    Then describe what the plan AIMED to produce (based on shipped evidence):
    - A WatcherAdapter-interface-compliant SlackAdapter that routes Slack
      events via the existing ntfy bridge (ADAPT-04 rule).
    - Event normalization with Sender formatted as `slack:{CHANNEL_ID}` so
      the router's `clients.json` can key on channel IDs.
    - Support for both v1 legacy and v2 structured payloads.
    - Thread reply routing via session_id lookup by parent dedup_key (per
      ADAPT-04 rule in REQUIREMENTS.md line 87).
    - Reconnect-with-backoff reusing the NtfyAdapter pattern from Plan 14-01.

    `<tasks>` section listing the 2 tasks that the commit history shows
    (one TDD pair per task):
    - Task 1 (RED+GREEN): failing Slack adapter tests in slack_test.go +
      implementation in slack.go with v1/v2 normalization and streamOnce
      reader.
    - Task 2 (RED+GREEN): reconnect + teardown + health tests + matching
      implementation wiring.

    Each task MUST include `<files>`, `<action>` (short — this is a
    reconstruction, not a prompt), and `<verify>` with the go test command.

    `<success_criteria>` section listing what the plan's output satisfies:
    ADAPT-04 closure, SlackAdapter exports Setup/Listen/Teardown/HealthCheck,
    events emitted with `slack:{CHANNEL_ID}` Sender format, stream reconnects
    on disconnect.

    HARD PROHIBITIONS: no speculation words (TODO / might / probably /
    likely). When describing intent, use "aims to" / "produces" / "shipped"
    — all grounded in observable post-ship code evidence.

    Minimum file length: 60 lines.
  </action>
  <verify>
    <automated>test -f .planning/phases/15-slack-adapter-and-import/15-01-PLAN.md &amp;&amp; grep -c "backfill: true" .planning/phases/15-slack-adapter-and-import/15-01-PLAN.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/15-slack-adapter-and-import/15-01-PLAN.md` exits 0.
    - `grep -c "backfill: true" 15-01-PLAN.md` >= 1 (frontmatter flag present).
    - `grep -c "ADAPT-04" 15-01-PLAN.md` >= 1.
    - `grep -c "SlackAdapter" 15-01-PLAN.md` >= 1.
    - `grep -E "\bTODO\b|\bmight\b|\bprobably\b|\blikely\b" 15-01-PLAN.md` returns zero matches.
    - `wc -l 15-01-PLAN.md` reports >= 60.
  </acceptance_criteria>
  <done>15-01-PLAN.md is written as a reconstructed plan-of-record with explicit backfill disclosure, ADAPT-04 as its sole requirement, and zero speculation words.</done>
</task>

<task type="auto">
  <name>Task 3: Write 15-01-SUMMARY.md (reconstructed Wave 1 summary matching 15-02-SUMMARY.md template)</name>
  <files>.planning/phases/15-slack-adapter-and-import/15-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/15-slack-adapter-and-import/15-02-SUMMARY.md (EXACT template to match — same frontmatter fields, same section headers, same tone)
    - .planning/phases/15-slack-adapter-and-import/15-01-PLAN.md (freshly written in Task 2)
    - Anchor table and commit list from Task 1
  </read_first>
  <action>
    Create `.planning/phases/15-slack-adapter-and-import/15-01-SUMMARY.md`
    mirroring the structure of the existing `15-02-SUMMARY.md` line-for-line.

    FRONTMATTER (YAML) — match 15-02-SUMMARY.md field-for-field:
    ```
    ---
    phase: 15-slack-adapter-and-import
    plan: 01
    subsystem: watcher
    tags: [slack, adapter, ntfy-bridge, channel-routing, event-normalization]
    backfill: true

    # Dependency graph
    requires:
      - phase: 13-watcher-engine-core
        provides: "WatcherAdapter interface, Event struct, AdapterConfig"
      - phase: 14-simple-adapters-webhook-ntfy-github
        plan: 01
        provides: "NtfyAdapter streaming and reconnect pattern (SlackAdapter reuses)"
    provides:
      - "SlackAdapter implementing WatcherAdapter via ntfy bridge"
      - "slack:{CHANNEL_ID} Sender format for routing via clients.json"
      - "v1/v2 Slack payload normalization"
    affects: [15-02 (watcher import uses same slack:{CHANNEL_ID} key format), 16-watcher-cli]

    # Tech tracking
    tech-stack:
      added: []
      patterns: ["NDJSON stream over ntfy bridge (shared with NtfyAdapter)", "v1/v2 payload dispatcher", "slack:{CHANNEL_ID} Sender format for router compatibility"]

    key-files:
      created:
        - internal/watcher/slack.go
        - internal/watcher/slack_test.go
      modified: []

    key-decisions:
      - "Route via ntfy bridge rather than native Slack Socket Mode — the Cloudflare Worker ntfy bridge already handles Slack Events API forwarding, so the adapter re-uses the NtfyAdapter NDJSON streaming pattern (ADAPT-04)"
      - "Sender format `slack:{CHANNEL_ID}` chosen so clients.json can route by channel ID without additional metadata"
      - "v1/v2 payload dispatcher keeps legacy Slack Events API v1 format working while new installations use v2 structured envelopes"

    requirements-completed: [ADAPT-04]

    # Metrics
    duration: <derived from commit timestamps in Task 1>
    completed: <derived from final commit date in Task 1>
    ---
    ```

    BODY — match the H1/H2 structure of 15-02-SUMMARY.md:
    1. H1 title: `# Phase 15 Plan 01: Slack Adapter Summary (backfill)`
    2. One-line abstract describing what shipped.
    3. `## Performance` — Duration, Started, Completed, Tasks, Files created
       (derived from git log).
    4. `## Accomplishments` — bullet list of concrete shipped capabilities,
       each anchored to a code location. Example:
       - "SlackAdapter implements WatcherAdapter (Setup/Listen/streamOnce/
         normalizeSlackEvent/Teardown/HealthCheck) at
         `internal/watcher/slack.go:<lines>`"
       - "v2 payload handler formats Sender as `slack:{CHANNEL_ID}` at
         `internal/watcher/slack.go:<line>`"
       - "v1 legacy handler falls back to `slack:unknown` at
         `internal/watcher/slack.go:<line>`"
       - "<N> tests in `internal/watcher/slack_test.go` all passing with -race"
    5. `## Task Commits` — list the actual commit SHAs derived in Task 1,
       labeled by their TDD phase (test → feat). Example:
         1. **Task 1 (RED): Add failing Slack adapter tests** — `<SHA>` (test)
         2. **Task 1 (GREEN): Implement Slack adapter** — `<SHA>` (feat)
       If the real history differs (e.g. only one combined commit), record
       what actually happened and note: "commit history shows a single
       combined commit rather than the preferred TDD pair; this is recorded
       as-is for backfill accuracy".
    6. `## Files Created/Modified` — list `internal/watcher/slack.go` and
       `internal/watcher/slack_test.go` with line counts and principal exports.
    7. `## Decisions Made` — mirror key-decisions from frontmatter.
    8. `## Deviations from Plan` — state:
       "Retrospective backfill; the PLAN (15-01-PLAN.md) was reconstructed
       after the code shipped, so deviations cannot be meaningfully recorded.
       Shipped behavior is the source of truth."
    9. `## Issues Encountered` — "None known at backfill time."
    10. `## User Setup Required` — "None. ntfy bridge reuses existing
        Cloudflare Worker topic; no new external account required."
    11. `## Next Phase Readiness` — "Plan 15-02 (watcher import) uses the same
        slack:{CHANNEL_ID} key format in its clients.json merge path at
        `cmd/agent-deck/watcher_cmd.go:<line>`."
    12. Footer: `---` then `*Phase: 15-slack-adapter-and-import*` and
        `*Completed: <date>*` and a line noting `*Reconstructed: <ISO8601>
        as part of REQ-WF-2 backfill*`.

    HARD PROHIBITIONS: no TODO/might/probably/likely anywhere in the doc.
    Every accomplishment bullet MUST have at least one path:line citation
    into internal/watcher/slack.go or internal/watcher/slack_test.go.

    Minimum file length: 50 lines.
  </action>
  <verify>
    <automated>test -f .planning/phases/15-slack-adapter-and-import/15-01-SUMMARY.md &amp;&amp; grep -c "requirements-completed: \[ADAPT-04\]" .planning/phases/15-slack-adapter-and-import/15-01-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/15-slack-adapter-and-import/15-01-SUMMARY.md` exits 0.
    - `grep -c "requirements-completed: \[ADAPT-04\]" 15-01-SUMMARY.md` >= 1.
    - `grep -c "backfill: true" 15-01-SUMMARY.md` >= 1.
    - `grep -c "internal/watcher/slack.go:" 15-01-SUMMARY.md` >= 2 (at least two specific citations).
    - `grep -c "slack:" 15-01-SUMMARY.md` >= 1 (the channel-ID format is documented).
    - `grep -E "\bTODO\b|\bmight\b|\bprobably\b|\blikely\b" 15-01-SUMMARY.md` returns zero matches.
    - `wc -l 15-01-SUMMARY.md` reports >= 50.
  </acceptance_criteria>
  <done>15-01-SUMMARY.md mirrors the structure of 15-02-SUMMARY.md with the backfill flag, cites at least two lines in internal/watcher/slack.go, and has zero speculation words.</done>
</task>

<task type="auto">
  <name>Task 4: Write 15-VERIFICATION.md with path:line citations covering Slack + watcher import; final anti-speculation gate; commit</name>
  <files>.planning/phases/15-slack-adapter-and-import/15-VERIFICATION.md</files>
  <notes>
    Task 4 only writes 15-VERIFICATION.md. STEP 2's anti-speculation grep
    reads 15-01-PLAN.md and 15-01-SUMMARY.md (produced by Tasks 2 and 3)
    but only conditionally back-edits them if speculation words are
    detected. In the non-conditional path those files are read-only here.
  </notes>
  <read_first>
    - .planning/phases/13-watcher-engine-core/13-VERIFICATION.md (structural template)
    - .planning/phases/15-slack-adapter-and-import/15-01-PLAN.md (freshly written in Task 2)
    - .planning/phases/15-slack-adapter-and-import/15-01-SUMMARY.md (freshly written in Task 3)
    - .planning/phases/15-slack-adapter-and-import/15-02-SUMMARY.md (already present; source for watcher-import narrative)
    - docs/WATCHER-COMPLETION-SPEC.md §REQ-WF-2 (rule to satisfy)
    - CLAUDE.md at repo root (commit signature + no --no-verify rule)
    - Anchor table from Task 1
  </read_first>
  <action>
    STEP 1 — Create
    `.planning/phases/15-slack-adapter-and-import/15-VERIFICATION.md`
    following the `13-VERIFICATION.md` template.

    FRONTMATTER (YAML):
    ```
    ---
    phase: 15-slack-adapter-and-import
    verified: <ISO8601 UTC>
    status: passed
    score: <N/N, where N is the total number of observable truths below>
    overrides_applied: 0
    ---
    ```

    H1: `# Phase 15: Slack Adapter + watcher import Verification Report`

    `## Goal Achievement` → `### Observable Truths` table with AT LEAST 7 rows:

    Row 1 — SlackAdapter implements WatcherAdapter interface: cite
    `internal/watcher/slack.go:<Setup line>` Setup, `:<Listen line>` Listen,
    `:<Teardown line>` Teardown, `:<HealthCheck line>` HealthCheck against the
    interface declaration in `internal/watcher/adapter.go:<line>`.

    Row 2 — Slack events stream via ntfy bridge (shared infrastructure with
    NtfyAdapter, ADAPT-04 rule): cite
    `internal/watcher/slack.go:<streamOnce line>` for the NDJSON reader and
    the ntfy bridge URL usage in Setup.

    Row 3 — slack:{CHANNEL_ID} routing key format — THE CENTRAL REQ-WF-2
    routing CLAIM. Cite the exact line in
    `internal/watcher/slack.go:<normalizeV2 line>` where
    `Sender: fmt.Sprintf("slack:%s", payload.Channel)` (or equivalent string
    formatter) appears. Also cite the test in
    `internal/watcher/slack_test.go:<line>` that asserts the emitted Event's
    Sender is `slack:<channel-id>`.

    Row 4 — v1/v2 payload dispatch: cite
    `internal/watcher/slack.go:<normalizeSlackEvent line>` for the branch
    logic, the v2 handler (Row 3 anchor) and the v1 legacy handler with
    `slack:unknown` fallback at `internal/watcher/slack.go:<line>`. Cite both
    the v1 and v2 parse tests in `internal/watcher/slack_test.go`.

    Row 5 — watcher import atomic merge via os.Rename (CLI-07 rule): cite
    `cmd/agent-deck/watcher_cmd.go:<os.Rename line>`. Cite the
    end-to-end / merge-existing test in
    `cmd/agent-deck/watcher_cmd_test.go:<line>`.

    Row 6 — watcher import rejects symlink inputs via os.Lstat — THE CENTRAL
    REQ-WF-2 symlink-rejection CLAIM. Cite
    `cmd/agent-deck/watcher_cmd.go:<os.Lstat line>` and the adjacent
    `return fmt.Errorf("symlink not allowed: %q", cleanPath)` line. Cite the
    symlink-rejection test in
    `cmd/agent-deck/watcher_cmd_test.go:<line>`. Explicitly note that
    `os.Lstat` (not `os.Stat`) is used so the symlink itself is inspected
    instead of followed — this is the threat-model-T-15-07 mitigation from
    15-02-SUMMARY.md made observable.

    Row 7 — watcher import writes `slack:{CHANNEL_ID}` keys into
    `clients.json` that match the Sender format emitted by SlackAdapter
    (closing the loop): cite
    `cmd/agent-deck/watcher_cmd.go:<clientKey fmt.Sprintf line>` and note
    that this string is identical to `internal/watcher/slack.go:<normalizeV2 line>`
    Sender format — same key format on both sides of the router.

    `### Required Artifacts` table: one row per shipped file
    (`internal/watcher/slack.go`, `internal/watcher/slack_test.go`,
    `cmd/agent-deck/watcher_cmd.go`, `cmd/agent-deck/watcher_cmd_test.go`)
    with Status=VERIFIED and Details = line counts + principal exports.

    `### Key Link Verification` table: one row per
    (From → To → Via → Status) tuple, e.g.:
    - slack.go → adapter.go (WatcherAdapter interface)
    - slack.go → ntfy.go (streamOnce pattern reuse, if visible)
    - watcher_cmd.go → slack.go (slack:{CHANNEL_ID} key format shared between
      import writer and adapter emitter)
    - watcher_cmd.go → stdlib os.Lstat + os.Rename

    `### Behavioral Spot-Checks` table:
    | Behavior | Command | Result | Status |
    |----------|---------|--------|--------|
    | Slack adapter tests pass with race detector | `go test ./internal/watcher/... -run TestSlack -race -count=1` | <captured output pass banner> | PASS |
    | Watcher import tests pass with race detector | `go test ./cmd/agent-deck/... -run Watcher -race -count=1` | <captured output pass banner> | PASS |
    | Packages build cleanly | `go build ./internal/watcher/... ./cmd/agent-deck/...` | Exit 0 | PASS |

    Run the go test commands in this task. Capture and paste their pass
    banners directly into the table.

    `### Requirements Coverage` table: two rows — ADAPT-04 and CLI-07 — each
    SATISFIED with a one-line pointer to the Observable Truth row that
    backs it.

    Closing footer.

    HARD PROHIBITIONS: no TODO/might/probably/likely anywhere. Every
    Evidence cell in the Observable Truths table MUST contain at least one
    `path:line` citation matching either
    `internal/watcher/[a-z_]+\.go:[0-9]+` or
    `cmd/agent-deck/watcher_cmd(_test)?\.go:[0-9]+`.

    STEP 2 — Run the final anti-speculation grep across ALL THREE docs
    produced by this plan:

      grep -E "\bTODO\b|\bmight\b|\bprobably\b|\blikely\b" \
        .planning/phases/15-slack-adapter-and-import/15-01-PLAN.md \
        .planning/phases/15-slack-adapter-and-import/15-01-SUMMARY.md \
        .planning/phases/15-slack-adapter-and-import/15-VERIFICATION.md

    If any matches, edit to remove and re-run until zero matches.

    STEP 3 — Commit all three new docs in a single signed commit. Per the
    repo-root CLAUDE.md mandate: no --no-verify on source commits; .planning
    commits honor the signature rule too. Stage ONLY the three new files
    (not the existing 15-02-SUMMARY.md).

    Commit message:

      docs(19-02): Phase 15 backfill — Slack adapter PLAN/SUMMARY + Phase 15 VERIFICATION

      Closes REQ-WF-2. Reconstructs the shipped Phase 15 Wave 1 Slack adapter
      plan and summary from code evidence (ADAPT-04), and writes the
      phase-level observable-truth verification ledger covering Slack adapter
      (slack:{CHANNEL_ID} routing) and watcher import (os.Lstat symlink
      rejection + os.Rename atomic merge, CLI-07). All claims cite path:line
      anchors in internal/watcher/slack.go and cmd/agent-deck/watcher_cmd.go.

      Committed by Ashesh Goplani
  </action>
  <verify>
    <automated>test -f .planning/phases/15-slack-adapter-and-import/15-VERIFICATION.md &amp;&amp; ! grep -qE "\bTODO\b|\bmight\b|\bprobably\b|\blikely\b" .planning/phases/15-slack-adapter-and-import/15-01-PLAN.md .planning/phases/15-slack-adapter-and-import/15-01-SUMMARY.md .planning/phases/15-slack-adapter-and-import/15-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/15-slack-adapter-and-import/15-VERIFICATION.md` exits 0.
    - `grep -c "Observable Truths" .planning/phases/15-slack-adapter-and-import/15-VERIFICATION.md` >= 1.
    - `grep -Ec "(internal/watcher/slack(_test)?\.go:[0-9]+|cmd/agent-deck/watcher_cmd(_test)?\.go:[0-9]+)" .planning/phases/15-slack-adapter-and-import/15-VERIFICATION.md` >= 7 (at least one citation per observable-truth row).
    - `grep -c "fmt.Sprintf(\"slack:%s\"" .planning/phases/15-slack-adapter-and-import/15-VERIFICATION.md` >= 1 (slack:{CHANNEL_ID} routing format documented).
    - `grep -c "os.Lstat" .planning/phases/15-slack-adapter-and-import/15-VERIFICATION.md` >= 1 (symlink-rejection primitive documented).
    - `grep -c "os.Rename" .planning/phases/15-slack-adapter-and-import/15-VERIFICATION.md` >= 1 (atomic merge primitive documented).
    - `grep -c "hmac.Equal" .planning/phases/15-slack-adapter-and-import/15-VERIFICATION.md` == 0 (HMAC belongs to Phase 14, not Phase 15 — wrong-phase leakage check).
    - `grep -E "\bTODO\b|\bmight\b|\bprobably\b|\blikely\b" .planning/phases/15-slack-adapter-and-import/15-01-PLAN.md .planning/phases/15-slack-adapter-and-import/15-01-SUMMARY.md .planning/phases/15-slack-adapter-and-import/15-VERIFICATION.md` returns zero matches (ROADMAP success criterion #5, Phase 15 portion).
    - `go test ./internal/watcher/... -run TestSlack -race -count=1 -timeout 120s` exits 0; `go test ./cmd/agent-deck/... -run Watcher -race -count=1 -timeout 120s` exits 0 (pass banners captured into Behavioral Spot-Checks table).
    - `git log -1 --pretty=%B | grep -c "Committed by Ashesh Goplani"` == 1.
    - `git log -1 --pretty=%B | grep -ciE "claude|co-authored-by"` == 0 (no Claude attribution).
    - `git log -1 --name-only` shows ONLY the three new files: `15-01-PLAN.md`, `15-01-SUMMARY.md`, `15-VERIFICATION.md` under `.planning/phases/15-slack-adapter-and-import/` (no `15-02-SUMMARY.md`, no unrelated files).
    - No `--no-verify` flag was passed to git commit (executor self-attests; hooks must no-op cleanly on a .planning-only commit).
  </acceptance_criteria>
  <done>All three Phase 15 backfill docs (15-01-PLAN.md, 15-01-SUMMARY.md, 15-VERIFICATION.md) are committed in a single signed commit, test suites for Slack and watcher import pass cleanly, anti-speculation grep across all three returns zero matches, and the commit is signed "Committed by Ashesh Goplani".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — documentation-only phase, no code paths modified, no new data flows
introduced. The three backfill docs are read-only text consumed by humans
and `grep`. The shipped Slack adapter and `watcher import` code already
shipped under Phase 15 and carry their own threat model in `15-02-SUMMARY.md`
(T-15-07 symlink rejection via `os.Lstat`); this plan merely makes that
mitigation observable.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02 | N/A | 15-01-PLAN.md, 15-01-SUMMARY.md, 15-VERIFICATION.md (Markdown docs) | not-applicable | Docs-only; no attack surface. ASVS L1 controls (auth, session mgmt, input validation, crypto, access control) are all not-applicable for this plan. Pre-existing Slack + watcher-import threat model is preserved as-is in `15-02-SUMMARY.md`. |
</threat_model>

<verification>
Phase-level success criteria for plan 19-02 (map to ROADMAP Phase 19 success
criterion #3 and the Phase 15 portion of #5):

1. `.planning/phases/15-slack-adapter-and-import/15-01-PLAN.md` exists and
   conforms to the GSD plan template (frontmatter with `requirements: [ADAPT-04]`,
   `<tasks>` block with at least 2 tasks matching the commit history for the
   shipped Slack adapter, `<verification>`, `<success_criteria>`, `<output>`).
2. `.planning/phases/15-slack-adapter-and-import/15-01-SUMMARY.md` exists and
   conforms to the GSD summary template (frontmatter with
   `requirements-completed: [ADAPT-04]`, metrics, task-by-task commit SHAs
   reconstructed from git log, evidence section quoting test output).
3. `.planning/phases/15-slack-adapter-and-import/15-VERIFICATION.md` exists
   with at least 7 observable-truth rows covering: WatcherAdapter interface
   implementation, ntfy bridge streaming, `slack:{CHANNEL_ID}` routing format
   (CENTRAL claim), v1/v2 payload dispatch, `watcher import` atomic merge via
   `os.Rename`, symlink rejection via `os.Lstat` (CENTRAL claim), and the
   key-format loop closure between import-writer and adapter-emitter.
4. Every claim in 15-VERIFICATION.md cites a `path:line` reference of the form
   `internal/watcher/slack(_test)?\.go:<N>` or
   `cmd/agent-deck/watcher_cmd(_test)?\.go:<N>` that resolves in the current
   tree.
5. Rerunning `go test ./internal/watcher/... -run TestSlack -race -count=1 -timeout 120s`
   and `go test ./cmd/agent-deck/... -run Watcher -race -count=1 -timeout 120s`
   both exit 0; pass banners are pasted verbatim into the Behavioral
   Spot-Checks table of 15-VERIFICATION.md.
6. `grep -E "\bTODO\b|\bmight\b|\bprobably\b|\blikely\b" .planning/phases/15-*/15-01-PLAN.md .planning/phases/15-*/15-01-SUMMARY.md .planning/phases/15-*/15-VERIFICATION.md`
   returns zero matches (ROADMAP success criterion #5, Phase 15 portion).
7. All three files are committed on `feat/watcher-completion` in a single
   commit signed `Committed by Ashesh Goplani` with no Claude attribution.
</verification>

<success_criteria>
- `test -f .planning/phases/15-slack-adapter-and-import/15-01-PLAN.md` → exit 0
- `test -f .planning/phases/15-slack-adapter-and-import/15-01-SUMMARY.md` → exit 0
- `test -f .planning/phases/15-slack-adapter-and-import/15-VERIFICATION.md` → exit 0
- `grep -c "Observable Truths" 15-VERIFICATION.md` >= 1
- `grep -Ec "(internal/watcher/slack(_test)?\.go:[0-9]+|cmd/agent-deck/watcher_cmd(_test)?\.go:[0-9]+)" 15-VERIFICATION.md` >= 7
- `grep -c "fmt.Sprintf(\"slack:%s\"" 15-VERIFICATION.md` >= 1 (routing format)
- `grep -c "os.Lstat" 15-VERIFICATION.md` >= 1 (symlink rejection)
- `grep -c "os.Rename" 15-VERIFICATION.md` >= 1 (atomic merge)
- `grep -E "\bTODO\b|\bmight\b|\bprobably\b|\blikely\b"` across all three docs returns no matches
- `go test ./internal/watcher/... -run TestSlack -race -count=1 -timeout 120s` exits 0
- `go test ./cmd/agent-deck/... -run Watcher -race -count=1 -timeout 120s` exits 0
- `git log -1 --pretty=%B | grep -c "Committed by Ashesh Goplani"` == 1
- `git log -1 --pretty=%B | grep -ciE "claude|co-authored-by"` == 0
- `git log -1 --name-only` lists exactly 3 files: 15-01-PLAN.md, 15-01-SUMMARY.md, 15-VERIFICATION.md
- REQ-WF-2 status in REQUIREMENTS.md can be flipped to `[x]` by a downstream maintainer (not this plan's job to flip it)
</success_criteria>

<output>
After completion, create
`.planning/phases/19-verification-docs-phases-14-15/19-02-SUMMARY.md` using the
standard GSD summary template. The SUMMARY must include:

- Frontmatter with `requirements-completed: [REQ-WF-2]`
- Metrics: duration, files modified (the three Phase 15 backfill docs)
- Task-by-task commit SHAs (one commit total for this plan, covering all
  three produced files)
- "Evidence reproduced" section quoting the final pass banners for
  `TestSlack*` and `Watcher*` test runs
- "Next phase readiness" note that REQ-WF-2 is closed and Phase 19 is
  therefore complete once 19-01 also lands (sibling in wave 1)
</output>